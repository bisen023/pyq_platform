<nav class="sticky top-0 z-20 border-b border-white/10 bg-slate-950/55 backdrop-blur-xl">
  <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 md:px-8">
    <%= link_to root_path, class: "group flex items-center gap-3" do %>
      <span class="flex h-11 w-11 items-center justify-center rounded-2xl border border-white/10 bg-white/5 text-sm font-black tracking-[0.28em] text-amber-300 shadow-lg shadow-slate-950/30 transition duration-300 group-hover:-translate-y-0.5 group-hover:bg-white/10">
        PYQ
      </span>
      <span class="hidden sm:block">
        <span class="block text-xs font-semibold uppercase tracking-[0.32em] text-slate-400">Exam archive</span>
        <span class="block text-lg font-semibold tracking-tight text-white">PYQ Platform</span>
      </span>
    <% end %>

    <div class="flex items-center gap-3 sm:gap-4">
      <% if user_signed_in? %>
        <span class="hidden rounded-full border border-emerald-400/20 bg-emerald-400/10 px-3 py-1 text-sm font-medium text-emerald-200 md:inline-block">
          <%= current_user.email %>
        </span>
        <%= button_to "Logout", destroy_user_session_path,
            method: :delete, form_class: "inline",
            class: "cursor-pointer rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm font-semibold text-white transition hover:border-red-300/30 hover:bg-red-500/15 hover:text-red-100" %>
      <% else %>
        <%= link_to "Login", new_user_session_path,
            class: "rounded-full px-3 py-2 text-sm font-semibold text-slate-200 transition hover:bg-white/5 hover:text-white" %>
        <%= link_to "Sign Up", new_user_registration_path,
            class: "rounded-full border border-amber-300/30 bg-amber-400 px-4 py-2 text-sm font-semibold text-slate-950 shadow-lg shadow-amber-950/20 transition hover:-translate-y-0.5 hover:bg-amber-300" %>
      <% end %>
    </div>
  </div>
</nav>
